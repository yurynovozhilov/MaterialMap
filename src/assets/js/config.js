/**
 * Configuration for Material MAP application
 * Basic configuration for read-only functionality
 */

window.MaterialMapConfig = {
    // Application settings
    app: {
        name: 'Material MAP',
        version: '1.0.0'
    }
};